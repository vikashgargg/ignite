apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - zelox.yaml

patches:
  - path: test-volume-patch.yaml
    target:
      kind: Deployment
      name: zelox-spark-server
