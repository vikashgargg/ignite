# Exclude large / sensitive directories that must never be in the build context.
# Using an exclude-list (not a ** catch-all) so that Apple Container's
# .dockerignore parser correctly passes sub-directory contents.
target/
.git/
python/
deployments/
docs/
scripts/
node_modules/
*.log
.env
